<template>
  <el-card :class="className" :style="{height:height,width:width}">
    <div slot="header" class="clearfix">
      <span class="card-title-text">产品客户数</span>
      <span class="card-title-unit">(个)</span>
    </div>
    <el-table
      :data="cardData"
      border
      class="card-main card-main-table card-product-cust-count"
      header-row-class-name="card-main-table-header"
      row-class-name="card-main-table-row">
      <el-table-column
        prop="productType"
        header-align="center"
        align="left"
        label="产品类型" width="95px"
        :resizable="false">
      </el-table-column>
      <el-table-column
        prop="personal"
        header-align="center"
        align="right"
        show-overflow-tooltip
        label="个人客户数"
        :resizable="false"
        :formatter="intColumnFormat">
      </el-table-column>
      <el-table-column
        prop="enterprise"
        header-align="center"
        align="right"
        show-overflow-tooltip
        label="对公客户数"
        :resizable="false"
        :formatter="intColumnFormat">
      </el-table-column>
      <el-table-column
        prop="total" show-overflow-tooltip
        header-align="center"
        align="right"
        show-overflow-tooltip
        label="总客户数" :resizable="false"
        :formatter="intColumnFormat">
      </el-table-column>
    </el-table>
  </el-card>
</template>

<script>
  import { productCustCount } from '../../../api/dashboard/dashboard'
  import { getYesterday } from '@eladmin/utils/data_format'

  export default {
    name: 'ProductCustCount',
    data() {
      return {
        cardData: []
      }
    },
    props: {
      className: {
        type: String,
        default: 'box-card'
      },
      width: {
        type: String,
        default: '100%'
      },
      height: {
        type: String,
        default: '100%'
      },
      autoResize: {
        type: Boolean,
        default: true
      },
      date: {
        type: String,
        default: getYesterday()
      }
    },
    mounted() {
      productCustCount(this.date).then(res => {
          this.cardData = res.data
        }
      )
    }
  }
</script>

<style scoped>
</style>
