<template>
  <div class="logo">
    <div class="sidebar-logo-container" :class="{'collapse':collapse}">
      <transition name="sidebarLogoFade">
        <router-link v-if="collapse" key="collapse" class="sidebar-logo-link" to="/">
          <img v-if="logo" :src="logo" class="sidebar-logo" style="margin-left:24px;">
          <h1 v-else class="sidebar-title">{{ title }} </h1>
        </router-link>
        <router-link v-else key="expand" class="sidebar-logo-link" to="/">
          <img v-if="logo" :src="logo" class="sidebar-logo">
          <h1 class="sidebar-title">{{ title }} </h1>
        </router-link>
      </transition>
    </div>
  </div>
</template>

<script>
import Config from '@public/config/settings'
import {mapGetters} from 'vuex'

export default {
  name: 'SidebarLogo',
  props: {
    collapse: {
      type: Boolean,
      required: true
    }
  },
  data() {
    return {
      title: Config.title,
      logo: Config.logo ? Config.logo : require('../../../../public/themes/' + this.getStyle()['style'] + '/images/logo.png')
    }
  },
  methods: {
    ...mapGetters(['getStyle'])
  }
}
</script>
<style lang="scss" scoped>
.sidebar-logo-container .sidebar-logo-link {
  background: transparent;
}
.sidebar-logo-container .sidebar-logo-link .sidebar-logo {
  background: transparent;
}
</style>
