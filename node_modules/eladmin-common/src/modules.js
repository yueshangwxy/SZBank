export default function loadView(view) {
  switch (view.substring(0, view.indexOf('/'))) {
    case '@eladmin':
      return (resolve) => require([`@/views/${view.substring(view.indexOf('/') + 1)}`], resolve)
    default:
      return (resolve) => require([`@/views/${view}`], resolve)
  }
}
