import { KeyValue } from '../types';
import { Timing, Interp } from '../common';
import { Cell } from './cell';
export declare class Animation {
    protected readonly cell: Cell;
    protected readonly ids: {
        [path: string]: number;
    };
    constructor(cell: Cell);
    get(): string[];
    start<T extends string | number | KeyValue<number>>(path: string | string[], target: T, options?: Animation.Options, delim?: string): number;
    stop(path: string | string[], delim?: string): this;
}
export declare namespace Animation {
    interface Options extends KeyValue {
        delay?: number;
        duration?: number;
        timing?: Timing.Names | Timing.Definition;
        interp?: Interp.Definition<any>;
    }
}
